import React from 'react';

const RightButton = ({ onClick }) => {
  return (
    <button onClick={onClick}>
      {/* Add SVG for right navigation button */}
    </button>
  );
};

export default RightButton;
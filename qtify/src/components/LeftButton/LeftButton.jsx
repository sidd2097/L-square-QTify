import React from 'react';

const LeftButton = ({ onClick }) => {
  return (
    <button onClick={onClick}>
      {/* Add SVG for left navigation button */}
    </button>
  );
};

export default LeftButton;